<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    commentable = Post.find(1)
	comments = commentable.comments.recent.limit(10).all
  </ol>
  <%= will_paginate @feed_items %>
<% end %>